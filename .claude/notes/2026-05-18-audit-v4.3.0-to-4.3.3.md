# v4.3.0 代码审计报告（M2）

> 📅 2026-05-18
> 🎯 目标：删冗余 / 减体积 / 提清晰度
> ⚖ 原则：**保守删，宁可留**（H6 + H1 区域不动）

---

## 一、确认冗余但**不能删**（依赖耦合）

| 文件 | 状态 | 不能删的原因 |
|----|----|----|
| `src/main/ipc/v2/lecture.handlers.js` | v3-9.5 单 lecture 模型旧版 | 仍在 `_registry.js` 注册中，被 `v2:getLectureStageData` / `v2:saveLectureStage` / `v2:confirmLectureStage` IPC 使用；下游 PPT/video 阶段卡片读取 lectureData 仍走这条；强行删需要联动改 8 处调用方 → 留到 D11 重构 |
| `src/main/v2/contracts.js` | H1 例外区，含 STAGE_ORDER 等核心契约 | 改这个必须老师明确批准（H1 已批 1 次：4 → 6 stage，再改 → 6 → 8 stage 需新批准）|
| `src/main/v2/quality.js` | 老 validateLectureStage / validatePptStage 函数 | D9 加了 per-lesson 模式开关向后兼容，仍被 single-lecture 路径调用 |
| `src/renderer/src/v2/LectureStage.jsx` 的 drafts 字段 | A/B/C UI 已删，但数据字段保留 | 老 artifact 还可能有 drafts.a 数据，删字段会让历史数据无法 load |
| `src/main/script/lecture-from-ppt-generator.js` | v4.2.0 Phase A'-6 引入 | 现在 D6.1 的 lessonGenerateDraft 走的是 lesson.handlers 内嵌 prompt，这文件可能已无人调用 → 标记待删，下版本确认无引用后删 |

---

## 二、确认冗余且**可以保守删**

无。**本轮不删任何文件**。

理由：v4.3.0 113 个 dirty 文件中已有大量修改，再删文件会让 commit 不可读；老师反馈「跨版本数据丢失」表明任何文件级删除都可能误伤老 artifact 加载路径。**M5 推 GitHub 前我会重新审视。**

---

## 三、3 处可在代码内**安全清掉**的注释/dead-code

仅修改单个函数体内的 unreachable code 或注释，不删文件、不改 export：

1. `LectureStage.jsx` 早期残留的 `if (false) { ... 动态练习 UI ... }` 块（D7 重构时折叠掉了，应彻底清掉）
2. `PptStage.jsx` 的 `dynamic-exercise` 相关代码（D1 删 PPT 阶段时遗留）
3. `V2App.jsx` 4 个 stub 函数：`saveFrameworkStage` / `handleGenerateFramework` / `handleConfirmFramework` / `handleExportFrameworkWord` —— P1.1d 注释里说"保留 stub 让残余引用不崩"，但 grep 后没找到任何残余引用了，**可删**

**本轮做哪个**：仅做 #3（V2App 4 个 framework stub），其他留到 M5。

---

## 四、巨石文件（LOC）

| 文件 | LOC | 状态 |
|----|----|----|
| `V2App.jsx` | 4460 | 🟥 过大，建议未来拆 modal 部分到 sub-component |
| `PptStage.jsx` | 1872 | 🟧 大但暂可接受 |
| `LectureStage.jsx` | 1535 | 🟧 D6-D8 多次重构，可拆 LectureChatPanel 已拆 |
| `index.js` (main) | 1479 | 🟥 包含太多业务函数，应迁到 services/ |
| `runtime.js` | 1044 | 🟧 v2 runtime 核心，可接受 |
| `quality.js` | 548 | 🟢 OK |

**本轮不重构巨石文件**，留 D11 专门处理。

---

## 五、本轮 M2 实际改动清单

| 改动 | 文件数 | 风险 |
|----|----|----|
| 1. 「我的工作台」→「教师日志」改名 | 5 | 极低，全局字符串替换 |
| 2. 删 V2App.jsx 4 个 framework stub | 1 | 低，grep 验证无引用 |
| 3. 加 `src/main/migrations/` 数据迁移目录 + 修 P0-1 老 design 数据丢失 | 2 | 中，新增目录不影响现有 |
| 4. 教师日志加「显示所有 artifact（含 status='deleted'）」开关 | 2 | 低，纯前端 toggle |

---

## 六、给 v4.3.3+ 的下一步建议

1. **D11 大重构 sprint**：
   - 拆 V2App.jsx 4460 行 → 多个 sub-component
   - 删 lecture.handlers.js v3 老版本
   - quality.js 重写支持 per-lesson 一等公民
2. **删 framework 残留**（contracts.js / runtime.js / workbench.handlers.js）
3. **加 ESLint 自定义规则**：禁 `|| 72` 类硬兜底（H14 自动化）
